---
title: "References"
permalink: "/docs/reference/"
layout: "layouts/page.njk"
description: "Baseline reference: plugin entrypoint, options, globals, filters, shortcodes, defaults, and automatic behaviors."
parent: "docs"
---

Baseline reference for when you need exact details: what the plugin registers by default, which options and globals it exposes, available filters/shortcodes, and the defaults it applies.

## Index
<ul>
{% for item in collections.docs %}
{% if item.data.topicSlug === topicSlug and item.url !== page.url %}
	<li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
{% endif %}
{% endfor %}
</ul>

Back to Docs: [/docs/](/docs/)
