import { describe, it } from 'vitest';

describe('createSeoNamespace', () => {
	it.todo('returns the raw seo object when no enrichment applies');
	it.todo('writes canonical, openGraph, twitter, and schema.graph onto the result');
	it.todo('caches into the scope under page.url for transform-time lookup');
});
